<% form_for @comment, url_for('', :author_id => @author.id, :post_id => @post.id, @comment.id => @comment.id, :method => 'PUT') do |form|%>

  <% field_set_tag do %>
    <%= form.label :body %>
    <%= form.text_field :body %>
  <%end%>

  <%= form.submit "Post"%>
<%end%>